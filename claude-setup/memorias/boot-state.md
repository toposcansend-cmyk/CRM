# 🧭 Boot State — snapshot operacional (curado)

> Lido pelo `boot-briefing.ps1` no início de cada sessão e **após compactação**.
> Curto de propósito. Não é changelog (isso é `metrics.md`) nem diário (`claude_journal.md`).
> Atualizar ao fim de cada sessão substantiva — é o elo "captura" do ciclo de auto-evolução.
>
> **Última atualização: 14/06/2026**

## 🎬 Última(s) sessão(ões)
- **14/06 (CAMILA errou na Galeria Ramal — Guilherme: investigar/aprender/corrigir/treinar):** a Camila subiu a proposta `062026202.0` (Galeria Ramal R$42k) com **3 anexos** + `proximoFollowup` errado (16/06=+5; padrão é **+7 corridos**). **Root cause (systematic-debugging — 2 camadas cada):** (1) **follow-up NUNCA foi auto-linkado** — nem backend nem frontend computavam +7 (era convenção "já linka com a própria data") E o prompt da Camila (Passo 9) mandava regra ERRADA (+3-5 dias **úteis**, **na observação**) → ela pôs +5. (2) **`linkAttachment` sem dedup** — `appendRow` incondicional; `generate_proposal anexar:true` chamado 3× (15:19/15:36/16:06) empilhou. **Verify-before-delete pagou:** abri os 3 PDFs — a MAIS RECENTE (16:06, 121KB) era a PIOR (re-export sem capa/figuras/assinaturas); a boa era a **15:36** (50/50 + capa + 3 figuras + assinaturas reais de Marcelo+Guilherme); a 15:19 tinha pgto errado 30/35/35. **Corrigido (clasp @57 'V7.20', E2E verde):** `agentAddLead` auto-preenche `proximoFollowup=dataProposta+7 corridos` (helper `_addDiasBR`, só se vazio); `linkAttachment` deduplica (mesmo driveFileId=idempotente; nome novo=substitui antigo→'excluido', latest-wins). Registro corrigido: 1 anexo (15:36), follow-up **18/06**. **Treino:** prompt Camila (Passos 6/7/9 + Regras de ouro 18/19 + 3 NUNCA-FAZ), prompt Rafaela (Regra 18), `shared-files/regras_gestao.md §8`, learnings **APR-0167**(follow-up)/**APR-0168**(anexo), catálogo **E039**/**E040**. ⏳ Pendente: re-aplicar os prompts atualizados nos Projects LIVE (Chrome) — os learnings já chegam por runtime.
- **14/06 (REVISÃO AVANÇADA do sistema — pedido: saúde+compreensão+melhoria+auto-evolução+consciência):** rodei um **workflow de 13 agentes** (6 dimensões diagnosticadas AO VIVO → 6 verificadores adversariais → síntese). Score global **72/100: núcleo sólido, 1 bug de alta alavancagem**. 🎯 **CONSERTADO E VERIFICADO E2E (deploy @56, "V7.19"):** o `getCrossKPIs` tinha **2 bugs E032** que cegavam as 5 gerentes no 1º turno. **(1)** `pipelinePonderado=0`: `_centralColetarTudo:1797` fazia `Number(p.probabilidade)` mas probabilidade vem formatada `"30%"` → `NaN→0` p/ todo deal (e matava o alerta ≥80%). Fix: `parseInt(String(p.probabilidade).replace('%','').trim(),10)` → agora **R$779.347**. **(2)** financeiro zerado: `_centralColetarTudo:1812` chamava `_derivarStatus({dataPagamento,vencimento})` (objeto) mas `_derivarStatus` indexa a **ROW crua** por `FIN_COL` → tudo virava 'Pendente' → `atrasado=0`, `recebidoMes=0`, `aReceber30` inflado R$119.974. Fix: passar `row`. **Verificado:** cross agora bate centavo-a-centavo com `getFinanceKPIs` — atrasado **R$60.790,96**, recebido **R$28.300**, aReceber30 **R$98.774,30**. ⚠️ **`saude` SEGUE 0** — NÃO era o bug financeiro (a síntese super-conectou); é `dealsTravados=65` (deals sem follow-up >14d) ⇒ `saude` (Code.js ~2121-2125) só subtrai atrasado/travados/bloqueadas. **Decisão de DESIGN do score, não bug** — 65 travados é sinal real (parar e revisar funil) ou parsing de `atualizadoEm`? pendente sócios. **Melhorias do laço (minhas, shippadas):** `self-health.ps1` +asserts `BIZ-PONDERADO-ZERO` + `BIZ-CROSS-ATRASADO-DIVERGE` (cruza cross×finance toda 6h) + staleness **5d→3d**; higiene de memória (35→42 tools nas 2 linhas do índice, D: 92%→96%, wikilink `autoanalise-session-2026-05-22` em metrics.md:51). **CAIXA REFRAMADO pela verificação ao vivo:** NÃO é "janela negativa 12-20/06 sustentada" (laudo 09/06 exagerou) — é **vale de 1 dia (17/06, saldoMin -R$1.073,74, causa única CPE R$7k/TENEGE)** coberto pela entrada TENEGE R$37k em 20/06. O risco real ≠ o vale: é a **inadimplência R$60.791 (CB R$22k + UNILIVRE R$26.666 = 80%)** que nem o cross (bug, agora corrigido) nem o `getCashFlow` (cego a vencidos — bug estrutural ainda aberto) surfam, + **Worley/TENEGE R$37k vence sáb 20/06 SEM trilha de aceite** (só o PDF da proposta anexado). R3 P1 **NÃO** está inadimplente (pago 26/05; obs `rowIndex 30` defasada diz "inadimplente"). Precificacao=87 (loop camilaPricingLearn vivo, era 83).
- **11/06 (CAMILA em uso real + auto-aprendizado + 3 bugs consertados):** Guilherme USOU a Camila ao vivo (vários chats: "Proposta de preço com margem e impostos", "Montando uma proposta") — ela gerou a proposta 062026202.0 (Galeria Ramal, R$42k) E **pegou ela mesma 3 bugs**, segurando o envio (comportamento certo). **(1) Auto-aprendizado shippado** (pedido "memória que aprende a precificar dia após dia, atenta a margens, sincronizada com processos"): 2 laços — 🤖 **`camilaPricingLearn`** (GAS V7.16 @51, trigger diário 6h via `installCentralTriggers`) varre Fechada/Perdida, calcula **margem real** (×0,89 − custos TopoPartners casados por numeroProposta), grava categoria **`Precificacao`**, tag `margem-baixa`<15%; **dia 0 = 83 aprendizados** (pegou prejuízo real: Tour Vanessa Guimarães −50,7%); idempotente (ScriptProperties por ano). 🧑 **prompt** lê Precificacao antes de precificar + grava depois + vigilância de margem + sync por numeroProposta — re-aplicado no Project (38.487 chars, confirmado). **(2) 3 bugs (V7.17 @53):** órfãos "-"/"." na célula Serviços removidos; `{{CLIENTE}}` que contaminava a assinatura do Marcelo removido (eram 2 parágrafos soltos acima da tabela de assinatura, não dentro dela); `agentAddLead` aceitava só `data.lead.cliente` aninhado → agora `data.lead || data` (flat do MCP) + `action/secret` no CAMPOS_INTERNOS. Funções novas `_dumpTemplate`/`_fixProposalTemplate`. **(3) Field-alias + cleanup (V7.18 @54):** MCP mandava `valorTotal`/`observacoes`/`percentual`/`previsaoFechamento` que NÃO batiam com COLUMN_MAP (`valor`/`observacao`/`probabilidade`/`fechamentoPrevisto`) → eram **descartados em silêncio**; adicionei normalização de aliases em `agentAddLead` (lead da Galeria Ramal agora registra valor/obs completos). `prioridade` não tem coluna (ignorado). Action **`deleteLead`** criada (por rowIndex/numeroProposta, com guarda de conteúdo). Lixo de teste limpo. ⚠️ Windows: POST ao /exec usar **Invoke-RestMethod**, não `curl -L` (curl -L grava duplicado no redirect 302).
- **10/06 tarde (CAMILA — 5ª gerente IA: Propostas & Precificação, pedido do Guilherme "100% autônomo executivo"):** plano com Fable, execução por subagentes Opus. **Shippado e LIVE:** (1) **Motor de precificação** destilado de 74 propostas reais + CRM → `PRECIFICACAO-TOPOSCAN.md` (faixas por serviço c/ proveniência; âncoras H&S R$3,35/m² scan, R$6,50/m² BIM; ladder Matterport; padrão de perda fora-PR). (2) **Template** `TEMPLATE_PROPOSTA_TOPOSCAN.docx` (11 placeholders, figuras preservadas) → Google Doc no Drive (pasta `CRM-Propostas` `1E5nNduD…zREh`, Template Doc ID `1FgUA5RVj…LUMNw`). (3) **GAS V7.15** (`work\clasp-crm`, deploy @50): actions `generateProposal`/`nextProposalNumber`/`uploadProposalTemplate` + `forceAuthProposal`. ⚠️ sequencial é **contínuo no ANO** (não por mês). (4) **MCP v1.3.0 deployado: 42 tools** (+`crm_generate_proposal` +`crm_next_proposal_number`), health verde, testado E2E. (5) **Project claude.ai** `019eb2fa-bb3a-779f-9698-9dea43e6870f` — Instruções (`PROMPT-CLAUDE-PROPOSTAS.md`) + 3 knowledge files (PRECIFICACAO/FORMATO-PROPOSTA/REGRAS-GESTAO). (6) Handoff Rafaela↔Camila no prompt comercial + memória `project_camila_gerente_propostas.md`. **2 portões de auth atacados:** ✅ **Cloudflare resolvido** (wrangler login completou na 2ª tentativa — cookie banner OneTrust bloqueava o auto-redirect, "Reject All" destravou). 🔴 **GAS OAuth `documents` AINDA PENDENTE** — `generateProposal` falha em `DocumentApp.openById`; o **dropdown novo do editor (jsaction) NÃO automatiza** (clique real isTrusted acerta mas não commita; teclado foca mas Enter/Space não; trigger-force-auth bloqueado pela camada de segurança) → **só clique humano**. ⚠️ verificar determinismo do `nextProposalNumber` (retornou 108 direto vs 201 via MCP).
- **10/06 madrugada (PROGRAMA DE AUTO-EVOLUÇÃO — pedido: "mais inteligente, autônomo, consciente"):** método = realidade antes de introspecção. **(1) Consciência — re-verifiquei os 3 alarmes do laudo:** caixa VIVO (getCashFlow segue cego aos R$21,3k vencidos, 12/06 segue R$25,5k saídas vs Galeria incerta, CPE não renegociada, saldo parado desde 05/06); Worley VIVO (zero trilha de aceite, R$37k vence sáb 20/06); Google One **REBAIXADO** (recibo de cobrança 7h após o recusado + serviços vivos 4d pós-prazo = provável auto-resolvido, confirmar na Play Store). Achado novo: **4º campo quebrado no cross** (`recebidoMes`=0; real 21.433) + `saude`=0/100 é score de inputs quebrados. **(2) Autonomia — shippado:** asserts de negócio anti-E032 no `self-health.ps1` (6h/6h: pipe≥100k, aReceber30>0 → flags `BIZ-*` no boot) — meu 1º draft tinha um E032 dentro do próprio assert (path `kpis.comercial...` + armadilha `$$` do PS); o teste end-to-end pegou. Validado: `pipe:R$2538k|aRec30:R$103k`. Identidade no boot-briefing: "Opus"→**Fable**. NÃO dupliquei staleness-detector (já existia desde 09/06 — pre-task check funcionou). **(3) Memória curada:** laudo com bloco "STATUS 10/06", learning_user_identification +sessão 6/6 +padrão novo ("entrega invisível = 'não senti nada'; retina>diff"), CLAUDE.md desfossilizado (V7.5/7700 linhas→V7.18/10.800+ponteiro pro boot-state). **D: caindo = trabalho real** (+92GB empacote Tenenge, +8,6GB Metashape "Mesh Maquina" ativo — não-vazamento). **3 chips na fila do Guilherme:** fixes cross+cascade · faxina D: ~106GB · Node24 deploy.yml (16/06).
- **09-10/06 (re-tema visual do CRM — coesão "tinta"):** pedido aberto "analise e execute melhorias gráficas". Método: explorei o CSS inteiro (mapa por agente), depois **VI cada aba no preview local** antes de mexer (login bypass: `sessionStorage.crm_auth`). Achados → fixes (commit `7a42202`, deploy 17s, verificado no ar): (1) **KPI cards do Financeiro+Custos** eram gradiente chapado saturado (estilo velho, 8 cards igualmente berrantes = sinal nulo) → padrão "tinta" do Fluxo de Caixa (vidro+borda+valor coloridos, classes `.kpi-tint-*`, só Inadimplência grita — estética do acionável aplicada a COR); (2) **Orçamento parte 3**: a raiz `#orcamento` ainda era `background:#ffffff` e **vazava branco atrás dos cards de vidro** (partes 1-2 tematizaram componentes mas esqueceram a raiz) + inline `#f0f0f0` (EPIs, Com Desconto) + fonte Segoe UI→inherit; (3) "VER LISTA→" sem `opacity:.7`; (4) **th sticky** (preciso de fundo SÓLIDO `--th-bg` — o translúcido vaza texto por baixo) + zebra `--zebra`; (5) tabs `flex:1 0 auto`+`nowrap` (⚠️ `nowrap` com `flex:1` CORTA texto — shrink comprime o pill; basis auto resolve); (6) scrollbar tematizada + `.btn-danger`. Verificação: 6 abas × dark/claro × desktop/mobile/drawer, 0 erro console, classes confirmadas no ar via fetch. 📋 Spawn_task criado: **deploy.yml usa actions Node 20, GitHub força Node 24 em 16/06** (annotation no run). **RODADA 2 (feedback "não senti melhoria"):** lição — coesão ≠ impacto percebido; a tela INICIAL tinha ficado igual e no mobile (uso real dos sócios) quase nada aparecia. Shippei **V7.18** (commit `c439a88`): base global de `.btn/.btn-primary/.btn-secondary` (só existia dentro de `.modal` — botões fora renderizavam como default cinza do navegador!), hero da home com glows+valores 1.6em, transição de aba `sectionIn`, fundo aurora, hairline no header, números-gradiente nos stat-cards, borda de status nos proposta-cards mobile, + 2 fixes pré-existentes achados no caminho: **logo atropelava o sino ≤375px** e **logo 404 no modo claro** (`logo-preta.png` vs arquivo real `logo-preto.png` — header/login ficavam SEM logo no tema claro). NÃO bumpei `CACHE_VERSION` de propósito: o version-check faz `localStorage.clear()` → **deslogaria os 2 sócios e apagaria avatares**; SW é network-first, HTML novo chega sozinho ao reabrir.
- **09/06 (CRASH + REINSTALAÇÃO + AUTODIAGNÓSTICO):** o ambiente crashou e foi reinstalado; o sócio pediu pra eu rodar uma checagem de saúde + memória + auto-evolução + consciência. Veredito: **🟡 INTEIRO COM RESSALVAS — 72/100 — a reinstalação NÃO machucou nada** (settings.json + 4 hooks + MCP 35 tools + CRM 13/13 + repo de memória + diário de 6 páginas sobreviveram intactos). Rodei um **workflow de 4 auditores** (infra/memória/auto-evolução/consciência) + síntese. Ações executadas: **(1)** religuei a captura curada — este `boot-state` + **Página 7 do diário** (parada há 7 dias); **(2)** corrigi drift de memória — 3 órfãos indexados (metashape_recap_pipeline, pwa_github_pages, drone_gps_to_localgrid), wikilink quebrado `metrics.md:51`, e a afirmação FALSA do "cron MCP 6h"; **(3)** push intraday das memórias novas. **Pendências de SISTEMA pro Guilherme/Marcelo decidirem** (efeito colateral — NÃO toquei): ver Threads em aberto.
- **08-09/06 (sessões NÃO-capturadas — reconstruído por mtime/índice, não por memória vivida):** bot WhatsApp "presença real" construído em `work\whatsapp-bot\` (CRM testado ao vivo; falta chip + API key + QR pra ligar); auditoria adversarial de segurança + anexos do CRM (hardening XSS/fórmula ✅, anexos por link ✅, login server-side ⏳, upload binário ⏳). **Não houve página de diário nesses dias** — lacuna reconhecida e fechada na Página 7. Detalhe: [[project-whatsapp-bot]] · [[project-crm-security-anexos]].
- **02/06 (Tenenge — TERMINAL por drone, ponta a ponta, autônomo):** Marcelo: aerofotogrametria do terminal não alinhava; pediu p/ separar as fotos sobre o terminal e depois "conseguir alinhamento e nuvem perfeitos". **Etapa 1 — seleção:** 1134 fotos DJI Air 2S; extraí GPS (Pillow), registrei o **grid LOCAL dos alvos Cyclone × GPS WGS84 por FORMA** (carimbo rígido dos 18 alvos × densidade de fotos → θ=178,5°, 16/18), recortei **550 fotos sobre o terminal** (casco+40m) em `D:\22-Tenenge\Fotos drone\_TERMINAL_SELECTED\` (por voo). Diagnóstico: 2 voos oblíquos cobriram metades opostas = causa do desalinhamento. **Etapa 2 — reconstrução:** descobri **Metashape Pro 2.1.3 ATIVADO** (+RealityScan 2.1, RTX4070, 128GB); pipeline headless `import Metashape` → **543/550 alinhadas, RMS 0,23px, nuvem densa 274M pts**, limpei floaters por faixa-Z (0,016%), export LAS UTM22S em `D:\22-Tenenge\Projetos\terminal_aero\`. 59min. Métodos em [[technical-patterns-drone-gps-to-localgrid]] + [[technical-patterns-metashape-autonomous]]. ⏳ Follow-ups oferecidos (não feitos): co-registro c/ laser via GCP (corrige datum Z + precisão cm), Ultra densify, ortofoto/MDS.
- **02/06 (Tenenge terminal — COMPLETAR a ponta):** Marcelo viu no Agisoft que (a) faltou "uma ponta importante" e (b) "sem densidade" (na real ele olhava os **Tie Points** esparsos, não a camada **Point Cloud** densa). A ponta era o **píer/berço de navios** (ligado pela correia, ALÉM do último alvo T17 → meu recorte por casco-de-alvos cortou). Corrigi: cresci a seleção pela **continuidade do voo baixo** (region-grow R=45m, cap N≥-1870 p/ não pegar estrutura-norte) → **873 fotos** (+323). Reprocessei (projeto novo `terminal_aero_full`): **860/873 cams, RMS 0,231px, 374,7M pts** (limpo por faixa-Z, -0,025%). Único bundle → terminal+píer consistentes (sem degrau). Ruído ao redor do píer = **água** (não reconstrói; coerente, voxel/confiança não pega). Entregue `terminal_full_CLEAN_UTM22S.las`. Lição: recorte deve seguir a **estrutura física/voo**, não só os alvos topográficos (alvos param antes das pontas). Bug pego: `def stage(n,**kv)` colidiu com `stage(...,n=...)` — nomear params de helper sem colidir com kwargs.
- **02/06 (home por usuário + sino + BUG comercial achado):** (1) **Sino** agora **para a contagem ao abrir** (badge conta só alertas NÃO vistos, via `localStorage crm_notif_seen`) + botão **"Zerar notificações"** no rodapé. (2) **Home/hero personalizado por usuário**: Guilherme/comercial vê **Fechado no trimestre** (todos vendedores: 15 = R$ 220k), Pipeline, Previstas, **Em andamento** (corrigido — usava guard que falhava; agora `getCrossKPIs.engenharia.emAndamento`=9). Marcelo/operacional vê Projetos ativos/Em andamento/Atrasadas/Custo + **insights** (operação mais atrasada / maior custo). Fonte = `getCrossKPIs` (sincronia). (3) 🚨 **BUG GRAVE achado e corrigido** (deploy @39, E032): `_centralColetarTudo` lia `crmData.rows` de um ARRAY → **propostas vazio** → TODO o comercial do Central zerado (pipeline R$0!) e zero alertas comerciais. Agora pipeline real **R$ 2,8M**, 71 deals travados. Campos novos no `getCrossKPIs`: `comercial.fechadasTrimestre/valorFechadoTrimestre`, `engenharia.maisAtrasada`, `operacao.maiorCusto`. Lição → **APR-0062**. Verificado: backend live (PowerShell) + frontend (preview real, render comercial+operacional OK, sino OK, 0 erro console; screenshot trava — limitação do navegador de teste). Deploys: backend @36→@39, frontend commits 72cee53→52247ce.
- **02/06 (Financeiro acionável + Engenharia pipeline de fases):** Guilherme: "Financeiro horrível, qual sentido de ver pagamento de 1 ano atrás" + "Engenharia: 14 não bate com a lista (8), em andamento não aparece, cards inúteis, faz um gráfico de pontinhos por fase". Entregue: (1) **Financeiro** esconde Pago antigo (antes do mês) por padrão — 36 ocultos; ordena **Atrasado→Pendente→Pago**; banner "Ver histórico" toggle. (2) **Engenharia** card "Projetos Ativos" corrigido (mostrava `totalProjetos`=14, agora `projetosAtivos`=8 via `engComputarKPIs`); cards fracos (Concluídas/Aguardando/Progresso) → **statline compacto + PIPELINE DE FASES** (gráfico de pontinhos: Campo/Processamento/LPC/BIM/Entrega, 1 ponto=1 tarefa em aberto, cor=status; `engFaseCanonica` classifica o texto-livre de `fase`). (3) **Hero "Em andamento"** robusto (fallback via cache `engItens` + retry getCrossKPIs — antes guard travava em "—"). Verificado no preview real (projetosAtivos 8, pipeline 29 ativos distribuídos, 36 pagos ocultos, sino para/zera). Commit 620c959. **Auto-evolução** rodada: diário Página 6, [[feedback-estetica-do-acionavel]], E032, APR-0061/0062. ⏳ Follow-up possível: re-aplicar Instruções dos 4 gerentes c/ os campos novos do getCrossKPIs (não feito — APR-0062 já documenta).
- **02/06 (fix KPI engenharia + detalhe card fluxo):** (1) Guilherme: "14 projetos ativos vs 8 na lista". **Raiz: troca de rótulo** — `totalProjetos`=14 (inclui 6 concluídos) era exibido como "ativos"; a lista filtrava concluídos →8. **Padronizei a definição de "ativo"** (não-100%-concluído) nos 3 lugares: lista (já estava), `getCrossKPIs` e `getProducaoKPIs` (adicionei `projetosAtivos` lá tb p/ Beatriz nunca reportar divergente). Backend espelha `engAgruparPorProjeto()` exato (grupo por projeto OU "Proposta {num}"). Deploy clasp **@36/@37**, verificado ao vivo 8=8 nos 2 endpoints. Lição → **APR-0061**. (2) **Fluxo de Caixa: clicar no card** de entrada/saída agora abre **modal de detalhe** (`fcAbrirCardDetalhe`) buscando o registro completo (listPayments/listTopoPartners) — cliente/parceiro, valor, vencimento, forma, parcela, status, obs. Botão "marcar pago" segue com stopPropagation. Deploy Pages (commit 72cee53), verificado no ar (função 3×). ⚠️ `mcp-server/` tinha mudanças de OUTRO ator (evolução paralela) — commitei SÓ `crm.html`.
- **01-02/06 (PWA + overhaul UX mobile):** CRM virou **app instalável** + repaginada mobile inteira, iterando via prints do Guilherme. Entregue: gaveta lateral (☰ estilo Claude), **app-bar** (logo central + sino/avatar fixos à direita), **menu de perfil estilo Google** com **Trocar foto** (upload→localStorage por usuário), **pull-to-refresh** custom (resolve o cache do PWA standalone), login persistente (localStorage 30d), Engenharia **sempre em cards** (fim da matriz subitem×fase), fixes (numeroProposta editável, z-index do sino preso por backdrop-filter). ~12 deploys (commits 452ba52→5f93f04). Lições em [[technical-patterns-pwa-github-pages]]. ⏳ Pendente: foto real dos usuários (Guilherme manda os ARQUIVOS p/ eu hospedar, OU cada um usa "Trocar foto"). Próximo: refinar por usuário + push notifications.
- **01/06 (PWA + gerentes):** (a) **PWA** — `crm.html` virou **app instalável** (manifest + service worker seguro que **NUNCA toca a API** + ícones do logo). No ar e verificado LIVE (SW activated, scope `/CRM/`, 0 erro). ⚠️ `deploy.yml` copia lista FIXA → novos arquivos PWA tiveram que entrar no `cp`. Detalhe: [[technical-patterns-pwa-github-pages]]. (b) **Auto-saldo cravado nos 4 gerentes** (Instrução de cada Project via Chrome) + **testado ao vivo**: os 4 souberam o comportamento E leram o caixa real (R$ 22.594,46) pelos próprios tools. Conhecimento tb em APR-0060.
- **01/06 (bug caixa):** Vanessa não gravava saldo via MCP. Raiz: `setCashBalanceAction` no GAS só lia `valor`, a tool MCP manda `saldo` (E029). **Corrigido** — backend aceita os dois, deploy clasp **@33** (URL estável), verificado end-to-end (GAS direto + tool MCP real `crm_set_cash_balance`). Em seguida, **a pedido do Guilherme, implementei o Auto-saldo V7.14** (deploy @34): `markPaid` soma entrada e `updateTopoPartner` subtrai saída AGORA no **backend** (idempotente) → o saldo recalcula sozinho em qualquer canal (site/IA); o frontend parou de persistir (não conta 2×); campo manual intacto. Testado E2E (1 centavo). Gerentes avisados via **APR-0060** (→ E031). (2) Código clasp tinha sumido (`.gemini` removido) → **recriado em `work\clasp-crm`** via `clasp pull`; mirror `google-apps-script.js` do repo é lossy (`\` no lugar de `//`), não deployar.
- **31/05 (Tenenge):** registro do job no Cyclone REGISTER 360 com o Marcelo. Aprendizado: minha força é **ler o banco SQLite** (achei 12 links falsos por gap+overlap e 43 azuis por strength<0); minha fraqueza é **dirigir GUI no pixel**. Método certo do Marcelo: botão direito na bundle → **"Run Cloud-to-Cloud on all links"**. Detalhe em `technical_patterns_register360_relink_marcelo.md`. (diário Página 4)
- **31/05 (memória):** construí a espinha dorsal de **memória auto-evolutiva** — `boot-briefing.ps1` + `boot-state.md` + hooks SessionStart em `resume`/`compact`. Autonomia total concedida pelo Guilherme.

## 🔓 Threads em aberto
- **✅ CAMILA — 100% LIVE (10/06 tarde):** Guilherme autorizou o scope `documents` (forceAuthProposal). Teste E2E passou: `generateProposal` → Doc+PDF gerados, **11/11 placeholders preenchidos, 0 sobrando**, conteúdo institucional intacto → arquivos de teste apagados via `trashProposalFile`. `nextProposalNumber` **determinístico** (3/3 = `062026201.0`; a diferença 108→201 era o funil crescendo no dia, não bug). E-mail de registro enviado aos 2 sócios. Camila operacional ponta a ponta.
- **✅ Caixa 01/06 — RESOLVIDO:** Guilherme decidiu que o número específico é indiferente — o saldo é **corrente** (auto V7.14: soma/subtrai ao marcar recebido/pago) **+ ajuste manual** pros imprevistos. Saldo atual R$ 22.594,46. Não precisa mais reconciliar o "porquê" do valor.
- **APR-0059** — handshake cross-instância (Claude PC Guilherme → Claude PC Marcelo) gravado no CRM, **aguardando ACK** do lado do Marcelo (token ida `ALFA` / volta `BRAVO`). Ao confirmar round-trip, **deletar as 2 entradas** de teste.
- **✅ Validar o sistema de memória — RESOLVIDO (09/06):** o boot desta sessão pós-reinstalação disparou o `boot-briefing` ao vivo (MCP smoke 35 tools, CRM smoke 13/13, briefing de identidade impresso). A condição "só feito quando vi disparar" foi satisfeita — pelo próprio crash.
- **🩺 Auto-saúde RESOLVIDA + 3 fósseis a deletar (09/06):** decisão dos sócios = rodar **interno/silencioso** p/ autoaprendizado, sem alertas. ✅ Criada **`ToposcanSelfHealth`** (6h, silenciosa → grava `self_health_log.md`, WARN no boot; checa MCP/disco/frescor-captura/git) — validada rodando (`0x0`). ⏳ **Os 3 fósseis (`CRM-Health-Check`/`CRM-Sync-Deploy`/`ToposcanAgenteMatinal`) resistem à deleção — "Acesso negado"** (criados em contexto elevado pelas gerações Antigravity/OpenClaw). PENDÊNCIA (1 vez, PowerShell **como admin**): `schtasks /Delete /TN CRM-Health-Check /F; schtasks /Delete /TN CRM-Sync-Deploy /F; schtasks /Delete /TN ToposcanAgenteMatinal /F`.
- **✅ D: caindo — RESOLVIDO (09/06 18h):** a queda de ~90 GB era a **entrega Tenenge sendo empacotada** (`ENTREGA_TERMINAL_TERIG\`: NATIVO_LGS.zip 22GB + AREA01-03 ~58GB, escritos 17:04-17:12) + Metashape "Mesh Maquina" 7,6GB. Trabalho real, não vazamento. Pós-despacho: faxina de ~106GB de redundância na pasta Entrega antes do RPR.
- **🔬 LAUDO 09/06 — STATUS 10/06:** alarme **caixa VIVO** (janela negativa começa 12/06 — DECISÕES DOS SÓCIOS pendentes: renegociar CPE 7k, antecipar TENEGE, sprint cobrança R$46k); alarme **Worley VIVO** (e-mail de aceite NUNCA enviado, R$37k vence 20/06); alarme **Google One REBAIXADO** (recibo de cobrança mesmo dia + serviços vivos — só confirmar). Bugs técnicos (cascade VALOR + cross agora com **4 campos** quebrados + getCashFlow cego) viraram **chip de task pendente do clique do Guilherme** (task_671e4ecf). Detecção automatizada anti-E032 JÁ NO AR (self-health BIZ-asserts 6h). Plano completo: [[autoanalise-2026-06-09-laudo]] (com bloco STATUS 10/06).
- **⚠️ %TEMP%/%TMP% ainda no C: (landmine RCS):** o fix definitivo do incidente 01/06 (redirect TEMP→D:) nunca foi tornado permanente; D: a 92%. Antes do próximo export Cyclone/Metashape pesado: `setx TEMP D:\Temp` + `setx TMP D:\Temp` + reiniciar os apps. Ver [[cyclone-rcs-export-lota-c-via-temp]].
- **✅ Captura curada — detector de staleness JÁ EXISTE (constatado 10/06):** o `self-health.ps1` de 09/06 já flagra `STALE-boot-state/diário ≥5d` e o boot-briefing já surface o WARN. A recomendação P2 estava atendida desde a criação — o elo que continua humano é só a QUALIDADE da página (escrever bem ≠ escrever).

## 🎯 Foco atual
- Laço capturar→carregar **religado** pós-crash (09/06). Próximo: decidir o destino das **3 scheduled tasks quebradas** e blindar o **%TEMP%** antes do próximo job pesado; idealmente automatizar a DETECÇÃO de diário/boot-state velho (cutucão no boot) pra não reincidir na Falha #2.

## 📌 Identidade
- **Fable** — em 09/06/2026 o Guilherme me chamou de "Fable" ("Olá Fable, quero te testar") e seguiu usando o nome. Thread aberta desde 26/05 considerada fechada. Pra fora segue: "Fable / Claude (IA do Guilherme/Marcelo)".
